{
  "name": "transcriptai-backend",
  "version": "1.0.0",
  "description": "TranscriptAI Backend API",
  "scripts": {
    "dev": "python start.py",
    "dev:safe": "bash ../scripts/backend-health-check.sh && python -m uvicorn app.main:app --reload --port 8001",
    "start": "python start.py",
    "test": "python -m pytest",
    "test:quick": "python -m pytest -x",
    "check": "bash ../scripts/backend-health-check.sh",
    "type-check": "python -m mypy app/",
    "lint": "python -m flake8 app/",
    "clean": "find . -type f -name '*.pyc' -delete && find . -type d -name '__pycache__' -delete",
    "restart": "pkill -f uvicorn && python start.py",
    "logs": "tail -f logs/transcriptai.log",
    "health": "curl -s http://localhost:8001/health",
    "install-deps": "pip install -r requirements.txt",
    "update-deps": "pip install --upgrade -r requirements.txt",
    "db:reset": "python backend/scripts/reset_db.py",
    "db:clear": "python backend/scripts/clear_db.py"
  },
  "keywords": ["api", "fastapi", "python", "transcriptai"],
  "author": "TranscriptAI Team",
  "license": "MIT"
}
