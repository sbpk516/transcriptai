{
  "name": "transcriptai-desktop",
  "version": "0.5.0",
  "private": true,
  "description": "TranscriptAI Electron desktop shell",
  "main": "src/main.js",
  "author": "TranscriptAI Team",
  "license": "UNLICENSED",
  "scripts": {
    "postinstall": "node scripts/postinstall.js",
    "dev": "concurrently -k -n VITE,ELECTRON -c \"magenta,cyan\" \"npm --prefix ../frontend run dev\" \"wait-on tcp:127.0.0.1:3000 && electron .\"",
    "dev:dictation-debug": "TRANSCRIPTAI_DICTATION_USE_CLIPBOARD=1 concurrently -k -n VITE,ELECTRON -c \"magenta,cyan\" \"npm --prefix ../frontend run dev\" \"wait-on tcp:127.0.0.1:3000 && electron .\"",
    "start": "electron src/main.js",
    "build:mac-key-server": "bash scripts/build-mac-key-server.sh",
    "pack": "npm run build:mac-key-server && npm --prefix ../frontend run build:electron && npm run prepack:check && electron-builder --dir -c electron-builder.yml",
    "prepack:check": "bash ../scripts/check-prepack.sh",
    "dist": "npm run build:mac-key-server && npm --prefix ../frontend run build:electron && npm run prepack:check && electron-builder -c electron-builder.yml",
    "dist:win": "npm --prefix ../frontend run build:electron && electron-builder --win nsis -c electron-builder.yml",
    "pack:win": "npm --prefix ../frontend run build:electron && electron-builder --dir --win -c electron-builder.yml",
    "test": "node --test"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.74",
    "@ai-sdk/google": "^3.0.67",
    "@ai-sdk/openai": "^3.0.58",
    "@nut-tree-fork/nut-js": "^4.2.6",
    "@nut-tree-fork/shared": "^4.2.6",
    "ai": "^6.0.174",
    "node-global-key-listener": "^0.3.0",
    "semver": "^7.6.3",
    "signal-exit": "^4.1.0",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "@electron/notarize": "^3.1.1",
    "concurrently": "^8.2.2",
    "electron": "38.7.2",
    "electron-builder": "^24.13.3",
    "electron-rebuild": "^3.2.9",
    "wait-on": "^7.2.0"
  }
}
