{
  "name": "transcriptai-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "prepare": "husky install",
    "scaffold": "tsx scripts/scaffold.ts",
    "lint": "npm --prefix frontend run lint && python -m flake8 backend/app",
    "type-check": "npm --prefix frontend run type-check && python -m mypy backend/app",
    "test": "python -m pytest backend/tests -q",
    "test:all": "python -m pytest -q",
    "build:dmg": "bash scripts/build-dmg-complete.sh",
    "build:dmg:clean": "bash scripts/build-dmg-complete.sh --clean",
    "build:dmg:force": "bash scripts/build-dmg-complete.sh --force",
    "build:dmg:minor": "bash scripts/build-dmg-complete.sh --minor",
    "build:dmg:major": "bash scripts/build-dmg-complete.sh --major",
    "build:dmg:no-bump": "bash scripts/build-dmg-complete.sh --no-bump",
    "release": "bash scripts/build-dmg-complete.sh --minor --publish",
    "release:patch": "bash scripts/build-dmg-complete.sh --publish",
    "release:major": "bash scripts/build-dmg-complete.sh --major --publish"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "tsx": "^4.19.0"
  }
}
